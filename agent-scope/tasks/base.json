{
  "id": "base",
  "description": "Shared exemptions for most tasks — extend via `inherits: [\"base\"]`.",
  "owner": "agent-scope",
  "allowed": [],
  "exemptions": [
    "**/dist/**",
    "**/build/**",
    "**/*.tsbuildinfo",
    "**/node_modules/**",
    "pnpm-lock.yaml",
    "package-lock.json",
    "yarn.lock",
    "**/coverage/**",
    "**/.turbo/**",
    "**/.next/**",
    "**/.vite/**"
  ],
  "notes": "This base task has no `allowed` patterns of its own. Child tasks must provide their own `allowed`. The base only contributes build-artifact / lockfile exemptions that apply to most tasks."
}
